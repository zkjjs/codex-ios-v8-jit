diff --git a/codex-rs/Cargo.toml b/codex-rs/Cargo.toml
index 17edbbfb2f1aabddde9dbd5a3192907f77f8369a..5c84b04ffd766b4df2ee410ebe10a001bbf44a93 100644
--- a/codex-rs/Cargo.toml
+++ b/codex-rs/Cargo.toml
@@ -267,7 +267,7 @@ mcp_test_support = { path = "mcp-server/tests/common" }
 age = "0.11.1"
 ansi-to-tui = "7.0.0"
 anyhow = "1"
-arboard = { version = "3", features = ["wayland-data-control"] }
+arboard = { version = "3" }
 arc-swap = "1.9.0"
 assert_cmd = "2"
 assert_matches = "1.5.0"
@@ -533,15 +533,15 @@ debug = "none"
 strip = "symbols"
 
 [profile.release]
-lto = "thin"
-debug = "line-tables-only"
+lto = "off"
+debug = "none"
 split-debuginfo = "off"
 # Keep release binaries symbolicateable until packaging has archived the
 # sidecar symbols and stripped the binaries.
 strip = false
 
 # Balance parallel release code generation against binary size.
-codegen-units = 4
+codegen-units = 16
 
 [profile.profiling]
 inherits = "release"
@@ -558,6 +558,7 @@ opt-level = 0
 [patch.crates-io]
 # Uncomment to debug local changes.
 # ratatui = { path = "../../ratatui" }
+v8 = { path = "../../rusty_v8" }
 crossterm = { git = "https://github.com/nornagon/crossterm", rev = "87db8bfa6dc99427fd3b071681b07fc31c6ce995" }
 ratatui = { git = "https://github.com/nornagon/ratatui", rev = "9b2ad1298408c45918ee9f8241a6f95498cdbed2" }
 tokio-tungstenite = { git = "https://github.com/openai-oss-forks/tokio-tungstenite", rev = "0e5b2d73aa18dd9f0a50ee9ff199d5aef7594186" }
diff --git a/codex-rs/cli/src/main.rs b/codex-rs/cli/src/main.rs
index baaa57ce0b4e03e8dfda29267cb42737b27e230d..89144c4f5d7ffe07644f138a96872dbf0e2fcf39 100644
--- a/codex-rs/cli/src/main.rs
+++ b/codex-rs/cli/src/main.rs
@@ -44,6 +44,12 @@ use std::path::PathBuf;
 use std::sync::Arc;
 use supports_color::Stream;
 
+// iOS compatibility: older jailbreak runtimes may not export the stack probe
+// emitted by newer Apple SDK toolchains.
+#[cfg(all(target_os = "ios", target_arch = "aarch64"))]
+#[unsafe(no_mangle)]
+pub extern "C" fn __chkstk_darwin() {}
+
 #[cfg(any(target_os = "macos", target_os = "windows"))]
 mod app_cmd;
 #[cfg(any(target_os = "macos", target_os = "windows"))]
diff --git a/codex-rs/process-hardening/src/lib.rs b/codex-rs/process-hardening/src/lib.rs
index f500e15d41487337fbb67ac35e19f4c9652f50f5..4c96985087eff256e1270bd88c033283a22b6e55 100644
--- a/codex-rs/process-hardening/src/lib.rs
+++ b/codex-rs/process-hardening/src/lib.rs
@@ -34,6 +34,7 @@ const PTRACE_DENY_ATTACH_FAILED_EXIT_CODE: i32 = 6;
     target_os = "linux",
     target_os = "android",
     target_os = "macos",
+    target_os = "ios",
     target_os = "freebsd",
     target_os = "netbsd",
     target_os = "openbsd"
diff --git a/codex-rs/code-mode/src/remote_session/connection.rs b/codex-rs/code-mode/src/remote_session/connection.rs
index 18ba5e0974..c954375846 100644
--- a/codex-rs/code-mode/src/remote_session/connection.rs
+++ b/codex-rs/code-mode/src/remote_session/connection.rs
@@ -1,9 +1,11 @@
+use std::collections::VecDeque;
 use std::fmt;
 use std::io;
 use std::path::Path;
 use std::path::PathBuf;
 use std::process::Stdio;
 use std::sync::Arc;
+use std::sync::Mutex;
 use std::sync::atomic::AtomicBool;
 use std::sync::atomic::Ordering;
 use std::time::Duration;
@@ -48,6 +50,7 @@ mod reader;
 
 const IPC_CHANNEL_CAPACITY: usize = 128;
 const HOST_HANDSHAKE_TIMEOUT: Duration = Duration::from_secs(10);
+const HOST_STDERR_TAIL_LINES: usize = 32;
 
 pub(super) enum ConnectionError {
     Spawn {
@@ -104,6 +107,8 @@ struct ConnectionSupervisor {
     driver_task: JoinHandle<()>,
     reader_task: JoinHandle<Result<(), String>>,
     writer_task: JoinHandle<Result<(), String>>,
+    stderr_task: Option<JoinHandle<()>>,
+    stderr_tail: Arc<Mutex<VecDeque<String>>>,
 }
 
 impl CallerCancellation {
@@ -147,12 +152,25 @@ impl Connection {
                 error,
             })?;
 
-        if let Some(stderr) = child.stderr.take() {
+        let stderr_tail = Arc::new(Mutex::new(VecDeque::with_capacity(
+            HOST_STDERR_TAIL_LINES,
+        )));
+        let stderr_task = child.stderr.take().map(|stderr| {
+            let stderr_tail = Arc::clone(&stderr_tail);
             tokio::spawn(async move {
                 let mut lines = BufReader::new(stderr).lines();
                 loop {
                     match lines.next_line().await {
-                        Ok(Some(line)) => debug!("code-mode host stderr: {line}"),
+                        Ok(Some(line)) => {
+                            debug!("code-mode host stderr: {line}");
+                            let mut tail = stderr_tail
+                                .lock()
+                                .unwrap_or_else(std::sync::PoisonError::into_inner);
+                            if tail.len() == HOST_STDERR_TAIL_LINES {
+                                tail.pop_front();
+                            }
+                            tail.push_back(line);
+                        }
                         Ok(None) => break,
                         Err(err) => {
                             warn!("failed to read code-mode host stderr: {err}");
@@ -160,8 +178,8 @@ impl Connection {
                         }
                     }
                 }
-            });
-        }
+            })
+        });
 
         let stdin = child
             .stdin
@@ -271,6 +289,8 @@ impl Connection {
                 driver_task,
                 reader_task,
                 writer_task,
+                stderr_task,
+                stderr_tail,
             }
             .run(),
         );
@@ -446,13 +466,39 @@ impl ConnectionSupervisor {
                 }
             }
         };
-        mark_connection_dead(&self.alive, &self.failure, reason.clone());
-        let _ = self.event_tx.try_send(DriverEvent::Failed(reason));
-        self.cancellation.cancel();
         if !child_exited {
             kill_and_reap(&mut self.child).await;
         }
+        if let Some(stderr_task) = self.stderr_task.take() {
+            let _ = stderr_task.await;
+        }
+        let stderr_tail = self.stderr_tail
+            .lock()
+            .unwrap_or_else(std::sync::PoisonError::into_inner)
+            .iter()
+            .cloned()
+            .collect::<Vec<_>>();
+        let reason = format_supervisor_failure(&reason, &stderr_tail);
+        mark_connection_dead(&self.alive, &self.failure, reason.clone());
+        let _ = self.event_tx.try_send(DriverEvent::Failed(reason));
+        self.cancellation.cancel();
+    }
+}
+
+fn format_host_exit(status: &str, stderr_tail: &[String]) -> String {
+    format_supervisor_failure(
+        &format!("code-mode host exited with status {status}"),
+        stderr_tail,
+    )
+}
+
+fn format_supervisor_failure(reason: &str, stderr_tail: &[String]) -> String {
+    let mut message = reason.to_string();
+    if !stderr_tail.is_empty() {
+        message.push_str("\ncode-mode host stderr:\n");
+        message.push_str(&stderr_tail.join("\n"));
     }
+    message
 }
 
 fn task_failure(
@@ -492,3 +538,47 @@ async fn kill_and_reap(child: &mut Child) {
     let _ = child.start_kill();
     let _ = child.wait().await;
 }
+
+#[cfg(test)]
+mod tests {
+    use super::format_host_exit;
+    use super::format_supervisor_failure;
+
+    #[test]
+    fn host_exit_includes_recent_stderr() {
+        let message = format_host_exit(
+            "signal: 5 (SIGTRAP)",
+            &[
+                "old diagnostic".to_string(),
+                "V8 fatal error at api.cc:123: resolver check failed".to_string(),
+            ],
+        );
+
+        assert_eq!(
+            message,
+            "code-mode host exited with status signal: 5 (SIGTRAP)\n\
+             code-mode host stderr:\n\
+             old diagnostic\n\
+             V8 fatal error at api.cc:123: resolver check failed"
+        );
+    }
+
+    #[test]
+    fn reader_eof_includes_recent_host_stderr() {
+        let message = format_supervisor_failure(
+            "code-mode host closed its stdout",
+            &[
+                "code-mode iOS runtime: tool-response: resolve promise".to_string(),
+                "V8 fatal error at api.cc:123: resolver check failed".to_string(),
+            ],
+        );
+
+        assert_eq!(
+            message,
+            "code-mode host closed its stdout\n\
+             code-mode host stderr:\n\
+             code-mode iOS runtime: tool-response: resolve promise\n\
+             V8 fatal error at api.cc:123: resolver check failed"
+        );
+    }
+}
diff --git a/codex-rs/code-mode/src/runtime/mod.rs b/codex-rs/code-mode/src/runtime/mod.rs
index 0d32858724..9ef8f7db15 100644
--- a/codex-rs/code-mode/src/runtime/mod.rs
+++ b/codex-rs/code-mode/src/runtime/mod.rs
@@ -256,7 +256,9 @@ fn run_runtime(
             RuntimeCommand::ObservePendingFrontier => {}
         }
 
+        module_loader::trace_ios_runtime_phase("tool-response: microtask checkpoint");
         scope.perform_microtask_checkpoint();
+        module_loader::trace_ios_runtime_phase("tool-response: microtask checkpoint complete");
         match module_loader::completion_state(scope, pending_promise.as_ref()) {
             CompletionState::Completed {
                 stored_value_writes,
diff --git a/codex-rs/code-mode/src/runtime/module_loader.rs b/codex-rs/code-mode/src/runtime/module_loader.rs
index 00185edb58..250078c589 100644
--- a/codex-rs/code-mode/src/runtime/module_loader.rs
+++ b/codex-rs/code-mode/src/runtime/module_loader.rs
@@ -68,6 +68,7 @@ pub(super) fn resolve_tool_response(
     id: &str,
     response: Result<JsonValue, String>,
 ) -> Result<(), String> {
+    trace_ios_runtime_phase("tool-response: lookup resolver");
     let resolver = {
         let state = scope
             .get_slot_mut::<RuntimeState>()
@@ -78,17 +79,23 @@ pub(super) fn resolve_tool_response(
 
     let tc = std::pin::pin!(v8::TryCatch::new(scope));
     let mut tc = tc.init();
+    trace_ios_runtime_phase("tool-response: reopen resolver");
     let resolver = v8::Local::new(&tc, &resolver);
     match response {
         Ok(result) => {
+            trace_ios_runtime_phase("tool-response: convert JSON");
             let value = json_to_v8(&mut tc, &result)
                 .ok_or_else(|| "failed to serialize tool response".to_string())?;
+            trace_ios_runtime_phase("tool-response: resolve promise");
             resolver.resolve(&tc, value);
+            trace_ios_runtime_phase("tool-response: promise resolved");
         }
         Err(error_text) => {
             let value = v8::String::new(&tc, &error_text)
                 .ok_or_else(|| "failed to allocate tool error".to_string())?;
+            trace_ios_runtime_phase("tool-response: reject promise");
             resolver.reject(&tc, value.into());
+            trace_ios_runtime_phase("tool-response: promise rejected");
         }
     }
     if tc.has_caught() {
@@ -100,6 +107,15 @@ pub(super) fn resolve_tool_response(
     Ok(())
 }
 
+#[cfg(target_os = "ios")]
+#[allow(clippy::print_stderr)]
+pub(super) fn trace_ios_runtime_phase(phase: &str) {
+    eprintln!("code-mode iOS runtime: {phase}");
+}
+
+#[cfg(not(target_os = "ios"))]
+pub(super) fn trace_ios_runtime_phase(_phase: &str) {}
+
 pub(super) fn completion_state(
     scope: &mut v8::PinScope<'_, '_>,
     pending_promise: Option<&v8::Global<v8::Promise>>,
diff --git a/codex-rs/code-mode/src/v8_init.rs b/codex-rs/code-mode/src/v8_init.rs
index 0d00d9fad5..d640d5093c 100644
--- a/codex-rs/code-mode/src/v8_init.rs
+++ b/codex-rs/code-mode/src/v8_init.rs
@@ -49,12 +49,20 @@ fn initialize_v8_with_mode(jit_mode: V8JitMode) -> Result<V8Initialization, Stri
     let platform = v8::new_default_platform(0, false).make_shared();
     v8::V8::initialize_platform(platform.clone());
     v8::V8::initialize();
+    #[cfg(target_os = "ios")]
+    v8::V8::set_fatal_error_handler(report_v8_fatal_error);
     Ok(V8Initialization {
         _platform: platform,
         jit_mode,
     })
 }
 
+#[cfg(target_os = "ios")]
+#[allow(clippy::print_stderr)]
+fn report_v8_fatal_error(file: &str, line: i32, message: &str) {
+    eprintln!("V8 fatal error at {file}:{line}: {message}");
+}
+
 impl V8JitMode {
     fn description(self) -> &'static str {
         match self {
